# Frontend Layer

Frontend UI assets are currently Jinja templates under the project-level
`templates/` directory and are rendered by the Flask backend.

This `frontend/` folder is reserved for future separation of UI assets.

